<!-- FULL HTML CODE -->
<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
  <title>Islamic History</title>
  <link href="https://fonts.googleapis.com/css2?family=Noto+Naskh+Arabic&family=Playfair+Display&display=swap" rel="stylesheet">
  <style>
    html { scroll-behavior: smooth; }
    body {
      margin: 0;
      font-family: 'Playfair Display', 'Noto Naskh Arabic', serif;
      background: url('https://www.transparenttextures.com/patterns/arabesque.png') repeat;
      background-color: #fdfcf8;
      color: #2e2e2e;
    }
    .site-header {
      background-color: #003d2c;
      color: white;
      padding: 1rem 2rem;
      display: flex;
      justify-content: space-between;
      align-items: center;
    }
    .site-header .logo {
      font-size: 1.7rem;
      font-weight: bold;
    }
    .site-header ul {
      list-style: none;
      display: flex;
      gap: 1rem;
      padding: 0;
      margin: 0;
    }
    .site-header a {
      color: white;
      text-decoration: none;
      font-weight: 500;
    }
    .auth {
      display: flex;
      gap: 1rem;
      align-items: center;
    }
    .auth span {
      font-weight: bold;
    }
    .hero {
      text-align: center;
      padding: 3rem 1rem;
      background: linear-gradient(to bottom, #e4f0ea 0%, #ffffff 100%);
    }
    .hero h1 {
      font-size: 2.5rem;
      color: #004d3a;
    }
    .quote {
      font-style: italic;
      color: #555;
      margin: 1rem auto 1.5rem;
      max-width: 700px;
    }
    .hero-img {
      width: 90%;
      max-width: 800px;
      border-radius: 10px;
      box-shadow: 0 4px 8px rgba(0, 0, 0, 0.2);
      margin: 1rem 0;
    }
    .search-box {
      margin-top: 1rem;
      margin-bottom: 2rem;
      position: relative;
    }
    .search-box input {
      padding: 0.5rem;
      width: 200px;
      border: 1px solid #ccc;
      border-radius: 5px;
    }
    .search-box button {
      padding: 0.5rem 1rem;
      background-color: #007a5a;
      color: white;
      border: none;
      border-radius: 5px;
      margin-left: 0.5rem;
      cursor: pointer;
    }
    #suggestions {
      position: absolute;
      left: 50%;
      transform: translateX(-50%);
      width: 200px;
      text-align: left;
      background-color: white;
      border: 1px solid #ccc;
      border-top: none;
      z-index: 10;
    }
    #suggestions div {
      padding: 5px;
      cursor: pointer;
    }
    #suggestions div:hover {
      background-color: #f0f0f0;
    }
    .card-section {
      display: grid;
      grid-template-columns: repeat(auto-fit, minmax(250px, 1fr));
      gap: 1.5rem;
      padding: 2rem;
    }
    .card {
      background-color: white;
      border-radius: 10px;
      overflow: hidden;
      box-shadow: 0 2px 8px rgba(0, 0, 0, 0.1);
      transition: transform 0.3s ease, box-shadow 0.3s ease;
      cursor: pointer;
    }
    .card:hover {
      transform: translateY(-5px);
      box-shadow: 0 10px 20px rgba(0, 0, 0, 0.15);
    }
    .card img {
      width: 100%;
      height: 180px;
      object-fit: cover;
    }
    .card h3 {
      text-align: center;
      padding: 1rem;
      color: #004d3a;
    }
    .comments {
      padding: 2rem;
      background-color: #f9f9f9;
    }
    .comments h2 {
      text-align: center;
    }
    textarea {
      width: 100%;
      height: 100px;
      padding: 1rem;
      margin-top: 1rem;
      border-radius: 8px;
      border: 1px solid #ccc;
      font-family: inherit;
      font-size: 1rem;
    }
    .comments button {
      margin-top: 0.5rem;
      background-color: #007a5a;
      color: white;
      padding: 0.5rem 1rem;
      border: none;
      border-radius: 5px;
      cursor: pointer;
    }
    .comment-entry {
      margin-top: 1rem;
      padding: 0.5rem;
      border-bottom: 1px solid #9b9898;
    }
    .comment-actions {
      margin-top: 0.5rem;
    }
    .comment-actions button {
      margin-right: 10px;
      font-size: 0.8rem;
    }
    .reply-box {
      margin-top: 0.5rem;
    }
    .reply-entry {
      margin-left: 1rem;
      padding: 0.3rem;
      border-left: 2px solid #ccc;
    }
    .site-footer {
      background-color: #003d2c;
      color: white;
      text-align: center;
      padding: 2rem 1rem;
    }
    .footer-img {
      width: 120px;
      margin-bottom: 1rem;
    }
  </style>

  <script>
    const topics = [
      "The Prophet’s Life",
      "Caliphate Era",
      "Golden Age",
      "Islamic Empires",
      "Notable Scholars",
      "Women in History",
      "Inventions"
    ];

    function showSuggestions() {
      const input = document.getElementById("searchInput").value.toLowerCase();
      const suggestionsBox = document.getElementById("suggestions");
      suggestionsBox.innerHTML = "";
      if (input.length > 0) {
        const filtered = topics.filter(topic => topic.toLowerCase().includes(input));
        filtered.forEach(match => {
          const div = document.createElement("div");
          div.textContent = match;
          div.onclick = () => {
            document.getElementById("searchInput").value = match;
            suggestionsBox.innerHTML = "";
          };
          suggestionsBox.appendChild(div);
        });
      }
    }

    function searchTopics() {
      const input = document.getElementById("searchInput").value.toLowerCase();
      alert('Search functionality coming soon for: ' + input);
    }

    function navigate(url) {
      window.location.href = url;
    }

    window.onload = () => {
      const authContainer = document.getElementById("auth");
      const username = localStorage.getItem("username");
      if (username) {
        authContainer.innerHTML = <span>Welcome, ${username}</span> <a href="#" onclick="logout()">Logout</a>;
      } else {
        const input = prompt("Enter username to log in or sign up:");
        if (input) {
          localStorage.setItem("username", input);
          location.reload();
        }
      }
    }

    function logout() {
      localStorage.removeItem("username");
      location.reload();
    }
  </script>
  <script>
  fetch('http://localhost:3000/')
    .then(response => response.text())
    .then(data => console.log("Backend says:", data))
    .catch(err => console.error("Error:", err));
</script>
</head>
<body>
  <header class="site-header">
    <div class="logo">Islamic History</div>
    <nav>
      <ul>
        <li><a href="#">Home</a></li>
        <li><a href="prophet.html">Prophet</a></li>
        <li><a href="caliphs.html">Caliphs</a></li>
        <li><a href="golden-age.html">Golden Age</a></li>
        <li><a href="empires.html">Empires</a></li>
        <li><a href="scholars.html">Scholars</a></li>
        <li><a href="women.html">Women</a></li>
      </ul>
    </nav>
    <div class="auth" id="auth"></div>
  </header>

  <section class="hero">
    <h4>Note! the website is currently under creation if you want to financially support email us</h4>
    <h1> Journey Through Islamic History</h1>
    <p class="quote">"Indeed, in their stories there is a lesson for those of understanding." (Qur'an 12:111)</p>
    <div class="search-box">
      <input type="text" id="searchInput" placeholder="Search topics..." oninput="showSuggestions()" />
      <button onclick="searchTopics()">Search</button>
      <div id="suggestions"></div>
    </div>
    <img src="Masjid-Al-Haram-Zamzam-Well.jpeg" alt="Islamic architecture" class="hero-img" />
  </section>

  <section class="card-section">
    <div class="card" onclick="navigate('all-history.html')">
      <img src="Ancient World Map.jpg" alt="All History by Story" />
      <h3>All History by Story</h3>
    </div>
    <div class="card" onclick="navigate('Prophet’s Life.html')">
      <img src="Beauty-edited.jpg" alt="Prophet’s Life" />
      <h3>The Prophet’s Life ﷺ</h3>
    </div>
    <div class="card" onclick="navigate('caliphs.html')">
      <img src="umayyad_mosque_637237.jpg" alt="Caliphate Era" />
      <h3>Caliphate Era</h3>
    </div>
    <div class="card" onclick="navigate('golden-age.html')">
      <img src="d.webp" alt="Golden Age" />
      <h3>Golden Age</h3>
    </div>
    <div class="card" onclick="navigate('empires.html')">
      <img src="images.png" alt="Islamic Empires" />
      <h3>Islamic Empires</h3>
    </div>
    <div class="card" onclick="navigate('scholars.html')">
      <img src="pngtree-microscope-with-lab-glassware-science-laboratory-research-and-development-concept-image_16413146.jpg" alt="Notable Scholars" />
      <h3>Notable Scholars</h3>
    </div>
    <div class="card" onclick="navigate('inventions.html')">
      <img src="optimized-muslim-inventions-ibn-alhaitham.jpg" alt="Islamic Inventions" />
      <h3>Inventions</h3>
    </div>
    <div class="card" onclick="navigate('women.html')">
      <img src="jihad.png" alt="Women in History" />
      <h3>Women in History</h3>
    </div>
  </section>

  <section class="comments">
    <h2>Leave a Comment</h2>
    <textarea id="commentBox" placeholder="Your thoughts..."></textarea>
    <button onclick="submitComment()">Submit</button>
    <div id="commentSection"></div>
  </section>

  <footer class="site-footer">
    <div class="footer-content">
      <img src="5b_1800x1800_8cf91c14-77f6-451c-bae1-4cf17703bcb1.webp" alt="Islamic Calligraphy" class="footer-img" />
      <p>&copy; 2025 Islamic History Archive | Built with ❤️</p>
    </div>
  </footer>
</body>
</html>